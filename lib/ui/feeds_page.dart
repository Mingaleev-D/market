import 'package:flutter/material.dart';

class FeedsPage extends StatelessWidget {
  static const routeName = '/feeds-page';
  const FeedsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
